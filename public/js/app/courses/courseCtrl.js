angular.module('courseApp.controllers')

.controller('CourseCtrl', function($scope, config, events, CourseService) {
	
	var initialize = function() {
		console.log('CourseCtrl is loaded.')
	}

	initialize()
})
